package me.expandedcuber.item

import net.minecraft.text.Text
import net.minecraft.util.Formatting

open class SkySword(settings: Settings) : SkyItem(settings) {
    init {
        itemType = "sword"
    }
}